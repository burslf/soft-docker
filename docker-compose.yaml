# Use postgres/example user/password credentials
version: '3.1'

services:

  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=burslf
      - POSTGRES_PASSWORD=genial
      - POSTGRES_DB=soft-docker
    ports:
      - '5432:5432'
    # volumes: 
    #   - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
    driver: local